import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  bookMark: {
    position: 'absolute',
    top: 2,
    right: 4,
    left: 4,
    bottom: 4,
    backgroundColor: '#F2C422'
  }
});

export default styles;
