import React from 'react';
import { View } from 'react-native';

import { TComment } from '@/models/types';

type TItemSubProps = { item: TComment };

export const ItemSub: React.FC<TItemSubProps> = ({ item }) => {

  return (
    <View>

    </View>
  )

}
