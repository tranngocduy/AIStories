import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 16,
    paddingRight: 16,
    paddingLeft: 16
  },
  empty: {
    height: 200
  }
});

export default styles;
