import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  view: {
    flex: 1,
    padding: 16,
    rowGap: 16
  },
  empty: {
    height: 200
  }
});

export default styles;
