import { StyleSheet } from 'react-native';
import { } from '@/theme';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF'
  },
  view: {
    flex: 1
  },
  scroll: {
    flexGrow: 1,
    paddingTop: 40
  }
});
